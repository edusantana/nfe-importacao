<%= form_with(model: nota, local: true,  multipart: true ) do |form| %>
  <% if nota.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nota.errors.count, "error") %> prohibited this nota from being saved:</h2>

      <ul>
      <% nota.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :titulo %>
  <%= form.text_field :titulo %>

  <%= form.label :planilha_itens %>
  <%= form.file_field :planilha_itens %>

    

  <div class="actions">
    <%= form.submit 'Salvar' %>
  </div>
<% end %>
